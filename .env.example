# Copy to .env and fill in to run the demo/example against the real HumanAuthn API.
# When unset, `npm run demo` runs against a local in-process mock server.
HUMANAUTHN_API_KEY=
HUMANAUTHN_BASE_URL=https://api.verifik.co
