# Copy to .env and fill in to run the demo/example against the real Verifik API.
# When VERIFIK_CLIENT_JWT is unset, `npm run demo` runs against a local mock server.
VERIFIK_CLIENT_JWT=
# Optional; defaults to https://api.verifik.co
HUMANAUTHN_BASE_URL=https://api.verifik.co
